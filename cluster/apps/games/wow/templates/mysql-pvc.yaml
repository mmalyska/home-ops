---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: wow-mysql
  namespace: wow
spec:
  storageClassName: ceph-block
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
