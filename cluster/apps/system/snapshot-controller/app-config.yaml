- enabled: "true"
  namespace: snapshot-controller
  syncPolicy:
    enabled: true
    selfHeal: true
    prune: false
  ignoreDifferences:
    - kind: Secret
      name: snapshot-validation-webhook-tls
      jsonPointers:
        - /data
