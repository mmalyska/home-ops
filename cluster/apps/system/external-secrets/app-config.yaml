- enabled: "true"
  namespace: external-secrets
  syncWave: "-5"
  syncPolicy:
    enabled: true
    selfHeal: true
    prune: false
  ignoreDifferences:
    - kind: Secret
      name: bitwarden-access-token
      jsonPointers:
        - /data
