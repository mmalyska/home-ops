apiVersion: v1
kind: Service
metadata:
  name: signal-cli
  namespace: hermes-agent
spec:
  type: ClusterIP
  selector:
    app: signal-cli
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
