apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: onlyoffice-jwt-secret
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: bitwarden
  refreshInterval: 1h
  target:
    name: onlyoffice-jwt-secret
    creationPolicy: Owner
  data:
    - secretKey: JWT_SECRET
      remoteRef:
        key: "881ffad9-b465-46f9-9253-b47c0072eed5" #gitleaks:allow #ONLYOFFICE_JWT_SECRET
