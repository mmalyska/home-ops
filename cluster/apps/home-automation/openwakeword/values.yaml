app-template:
  controllers:
    openwakeword:
      strategy: Recreate
      containers:
        maniek:
          image:
            repository: rhasspy/wyoming-openwakeword
            tag: 2.1.0@sha256:52cb1168731a1849fc28cf339c935fde58746bbabc94226668a40ef6ddf5d42b
            pullPolicy: IfNotPresent
          args:
            - --custom-model-dir
            - /custom
          resources:
            requests:
              cpu: 500m
              memory: 200Mi
            limits:
              cpu: 2000m
              memory: 1Gi
          probes:
            liveness:
              enabled: true
            readiness:
              enabled: true
            startup:
              enabled: true
      initContainers:
        load-model:
          image:
            repository: public.ecr.aws/docker/library/alpine
            tag: latest@sha256:5b10f432ef3da1b8d4c7eb6c487f2f5a8f096bc91145e68878dd4a5019afde11
            pullPolicy: IfNotPresent
          command:
            - "/bin/sh"
            - -c
          args:
            - cd /custom && apk --no-cache add curl && curl -sLO https://raw.githubusercontent.com/mmalyska/home-ops/main/cluster/apps/home-automation/openwakeword/hey_maniek.tflite
  service:
    maniek:
      annotations:
        lbipam.cilium.io/sharing-key: "voice-pipeline"
        lbipam.cilium.io/sharing-cross-namespace: "*"
        lbipam.cilium.io/ips: "192.168.48.27"
      type: LoadBalancer
      controller: openwakeword
      ports:
        tcp:
          port: 10400
  persistence:
    custom:
      type: emptyDir
