app-template:
  controllers:
    piper:
      strategy: Recreate
      containers:
        pl:
          image:
            repository: rhasspy/wyoming-piper
            tag: 2.5.1@sha256:a61b12575e5a05b307983c3172bdf03ef7dd68a6f6093f3b6105a0c65004d0a3
            pullPolicy: IfNotPresent
          args:
            - --voice
            - pl_PL-darkman-medium
            - --update-voices
          resources:
            requests:
              cpu: 100m
              memory: 1Gi
            limits:
              cpu: 4000m
              memory: 2Gi
          probes:
            liveness:
              enabled: true
            readiness:
              enabled: true
            startup:
              enabled: true
  service:
    pl:
      annotations:
        lbipam.cilium.io/sharing-key: "voice-pipeline"
        lbipam.cilium.io/sharing-cross-namespace: "*"
        lbipam.cilium.io/ips: "192.168.48.27"
      type: LoadBalancer
      controller: piper
      ports:
        tcp:
          port: 10200
  persistence:
    data:
      type: persistentVolumeClaim
      accessMode: ReadWriteOnce
      size: 10Gi
